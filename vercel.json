{
  "version": 2,
  "builds": [
    { "src": "api/index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.js" }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/backend/api/$1" }
  ],
  "root": "backend"
}
